<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@drawable/bg_gradient">

    <!-- Back Arrow and Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="24dp">

        <ImageView
            android:id="@+id/backArrow"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/arrowleft"
            android:contentDescription="Back"
            app:tint="@color/main" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Attendance"
            android:textSize="28dp"
            android:textAlignment="center"
            android:paddingLeft="64dp"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Illustration -->
    <LinearLayout
        android:id="@+id/cardLayout"
        android:layout_width="wrap_content"
        android:layout_height="600dp"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:layout_marginTop="24dp"
        android:background="@drawable/glass_card_bg"
        android:elevation="8dp"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:layout_width="300dp"
            android:layout_height="200dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="-20dp"
            android:contentDescription="Attendance Illustration"
            android:src="@drawable/calendarbro" />

        <!-- Date Picker -->
        <EditText
            android:id="@+id/dateEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="@drawable/edittext_glass_bg"
            android:drawableEnd="@drawable/calendar"
            android:drawablePadding="12dp"
            android:focusable="false"
            android:hint="Select Date"
            android:inputType="date"
            android:padding="14dp"
            android:textColor="#6F6969"
            android:textSize="16sp" />

        <!-- Attendance List Card Placeholder -->
        <TextView
            android:id="@+id/attendanceListTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Attendance List"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Scrollable Attendance List Section -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            android:layout_weight="1">

            <TextView
                android:id="@+id/attendanceTextView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="10dp"
                android:text="(Attendance will appear here...)"
                android:textColor="@color/white"
                android:textSize="16dp" />
        </ScrollView>


    </LinearLayout>
</LinearLayout>